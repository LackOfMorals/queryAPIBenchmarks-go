
docker run -dt --name haproxy01 \
--ulimit nofile=200000:200000 \
--network host \
--volume=$HOME/haproxy01:/usr/local/etc/haproxy:rw \
--user="$(id -u):$(id -g)" \
--restart=always \
haproxy:alpine

